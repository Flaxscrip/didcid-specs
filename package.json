{
  "name": "did-cid-spec",
  "version": "0.1.0",
  "description": "did:cid Method Specification — DIF Spec-Up",
  "private": true,
  "scripts": {
    "render": "node render.js",
    "edit": "node -e \"require('spec-up')()\"",
    "build": "node render.js"
  },
  "dependencies": {
    "spec-up": "^0.10.8"
  }
}